<Project>
  <!-- Intentionally empty. Shields godot-client from the repo-root Directory.Build.targets: MSBuild uses only the NEAREST Directory.Build.targets up the tree. Do not add game/spirectl wiring here. -->
</Project>
